import React from "react"

import * as styles from "./footer.module.css"

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p><em>Insert inspirational quote here.</em></p>
        </footer>
    )
};

export default Footer;
