import * as React from "react"
import Layout from "../components/layout";

const NotFound = () => {
    return (
        <Layout title="Not found">
            <p>Page not found - 404 <span role="img" aria-label="Dead">😵</span></p>
        </Layout>
    )
};

export default NotFound;
